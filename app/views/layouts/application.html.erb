<!DOCTYPE html>
<html>
<head>
  <title>RoboJS</title>
  <%= stylesheet_link_tag 'application' %>
  <link href='http://fonts.googleapis.com/css?family=Open+Sans:300|Sarpanch' rel='stylesheet' type='text/css'>
  <%= javascript_include_tag 'application' %>
  <script src="//js.pusher.com/2.2/pusher.min.js" type="text/javascript"></script>
  <%= csrf_meta_tags %>
</head>
<body>
  <header>
  <div class="general-headers">
    <div class="profile">
      <% if current_user %>
        <%= link_to "Profile", profile_path(current_user), class: "user-navigation" %>
      <% end %>
    </div>

    <h1 class="layout-header">Robo.Js</h1>
    <div class="logout">
      <% if current_user %>
        <%= link_to "Log Out", logout_path, method: "delete", class: "user-navigation" %>
      <% end %>
    </div>
  </div>
  </header>
  <div class="container">
    <%= yield %>
  </div>
</body>
</html>